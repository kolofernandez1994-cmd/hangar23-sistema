<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <meta name="theme-color" content="#ffdb59">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <title>HANGAR 23 · Pedidos</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=DM+Mono:wght@400;500&family=DM+Sans:wght@400;500;700&display=swap" rel="stylesheet">
  <style>
    :root{--bg:#0a0a0a;--surface:#151515;--surface2:#202020;--line:#303030;--yellow:#ffdb59;--text:#f7f7f7;--muted:#969696;--green:#47ff8e;--danger:#ff6565}
    *{box-sizing:border-box} body{margin:0;background:var(--bg);color:var(--text);font:15px 'DM Sans',sans-serif;padding:env(safe-area-inset-top) 0 env(safe-area-inset-bottom)}
    .app{max-width:620px;margin:auto;min-height:100vh;padding:18px 16px 100px}.top{display:flex;justify-content:space-between;align-items:center;margin-bottom:25px}.brand{font:32px 'Bebas Neue',sans-serif;letter-spacing:1.5px;color:var(--yellow);line-height:.85}.brand small{display:block;font:10px 'DM Mono',monospace;letter-spacing:1px;color:var(--muted);margin-top:7px}.status{font:10px 'DM Mono',monospace;color:var(--green)}
    h2{font:25px 'Bebas Neue',sans-serif;letter-spacing:1px;margin:0 0 12px}.card{background:var(--surface);border:1px solid var(--line);border-radius:14px;padding:15px;margin-bottom:16px}.field{margin-bottom:13px}.field:last-child{margin-bottom:0}label{font:10px 'DM Mono',monospace;letter-spacing:.7px;color:var(--muted);display:block;margin:0 0 6px;text-transform:uppercase}input,select,textarea{width:100%;border:1px solid var(--line);border-radius:10px;background:var(--surface2);color:var(--text);font:15px 'DM Sans',sans-serif;padding:12px;outline:none}input:focus,select:focus,textarea:focus{border-color:var(--yellow)}textarea{min-height:70px;resize:vertical}select option{background:#202020}
    .products{display:grid;grid-template-columns:repeat(2,1fr);gap:9px}.product{appearance:none;text-align:left;border:1px solid var(--line);border-radius:11px;background:var(--surface2);color:var(--text);padding:12px;min-height:74px;font:500 15px 'DM Sans',sans-serif}.product.active{border-color:var(--yellow);box-shadow:inset 0 0 0 1px var(--yellow)}.product b{display:block;font-size:13px;margin-top:5px;color:var(--yellow);font-family:'DM Mono',monospace}
    .extras{display:flex;flex-wrap:wrap;gap:8px}.extra{border:1px solid var(--line);border-radius:20px;background:var(--surface2);color:var(--text);padding:8px 10px;font:12px 'DM Sans',sans-serif}.extra.active{background:rgba(255,219,89,.12);border-color:var(--yellow);color:var(--yellow)}
    .add-row{display:flex;gap:10px;align-items:end;margin-top:14px}.qty{width:94px;flex:none}.btn{border:0;border-radius:10px;padding:13px;background:var(--yellow);color:#111;font:700 14px 'DM Sans',sans-serif;cursor:pointer;flex:1}.btn:disabled{opacity:.45}.btn.secondary{background:var(--surface2);border:1px solid var(--line);color:var(--text);flex:initial;padding:8px 10px}.cart-empty{color:var(--muted);padding:5px 0}.cart-row{display:flex;gap:8px;justify-content:space-between;padding:11px 0;border-bottom:1px solid var(--line)}.cart-row:last-child{border:0;padding-bottom:0}.cart-detail{font-size:12px;color:var(--muted);margin-top:3px}.total{display:flex;justify-content:space-between;align-items:center;margin:15px 0 0;padding-top:13px;border-top:1px solid var(--line);font:24px 'Bebas Neue',sans-serif;letter-spacing:1px}.total span{color:var(--yellow)}
    .submit{position:fixed;left:0;right:0;bottom:0;background:linear-gradient(transparent,#0a0a0a 23%);padding:25px 16px calc(15px + env(safe-area-inset-bottom));z-index:3}.submit .btn{max-width:588px;display:block;margin:auto;width:100%;font-size:16px}.recent{font-size:13px}.recent-item{border-bottom:1px solid var(--line);padding:10px 0;display:flex;justify-content:space-between;gap:10px}.recent-item:last-child{border:0}.recent-item small{display:block;color:var(--muted);margin-top:3px}.toast{position:fixed;top:20px;left:50%;transform:translate(-50%,-12px);opacity:0;transition:.2s;background:#fff;color:#111;border-radius:10px;padding:11px 14px;font-weight:700;z-index:10;pointer-events:none}.toast.show{opacity:1;transform:translate(-50%,0)}.hidden{display:none!important}
  </style>
</head>
<body>
  <main class="app">
    <header class="top"><div class="brand">HANGAR 23<small>PEDIDOS · MÓVIL</small></div><div id="status" class="status">● CONECTANDO</div></header>
    <section class="card">
      <div class="field"><label for="cliente">Cliente</label><input id="cliente" autocomplete="name" placeholder="Nombre del cliente"></div>
      <div class="field"><label for="pago">Forma de pago</label><select id="pago"><option>Efectivo</option><option>Transferencia</option><option>Mercado Pago</option><option>Débito</option><option>Crédito</option></select></div>
    </section>
    <section class="card"><h2>Elegí el producto</h2><div id="productos" class="products"><span class="cart-empty">Cargando menú…</span></div></section>
    <section id="extras-card" class="card hidden"><h2>Extras</h2><div id="extras" class="extras"></div></section>
    <section class="card">
      <div class="add-row"><div class="field qty"><label for="cantidad">Cantidad</label><input id="cantidad" type="number" inputmode="numeric" min="1" value="1"></div><button id="agregar" class="btn" disabled>+ Agregar al pedido</button></div>
    </section>
    <section class="card"><h2>Pedido</h2><div id="carrito" class="cart-empty">Todavía no agregaste productos.</div><div class="total">Total <span id="total">$0</span></div></section>
    <section class="card"><div class="field"><label for="notas">Notas para cocina</label><textarea id="notas" placeholder="Ej.: sin cebolla, retirar por mostrador…"></textarea></div></section>
    <section class="card"><h2>Últimos pedidos</h2><div id="recientes" class="recent"><span class="cart-empty">Cargando…</span></div></section>
  </main>
  <footer class="submit"><button id="confirmar" class="btn" disabled>Confirmar pedido · $0</button></footer>
  <div id="toast" class="toast"></div>
  <script type="module">
    import { initializeApp } from 'https://www.gstatic.com/firebasejs/10.12.2/firebase-app.js';
    import { getFirestore, collection, onSnapshot, addDoc, runTransaction, doc } from 'https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore.js';
    const firebaseConfig={apiKey:'AIzaSyDYvEr5rbVjhYLX5sbqYh4pK9bDSOH1cyk',authDomain:'hangar23-v706.firebaseapp.com',projectId:'hangar23-v706',storageBucket:'hangar23-v706.firebasestorage.app',messagingSenderId:'1073514334115',appId:'1:1073514334115:web:22de1feb1e94127857bc16'};
    const fsDb=getFirestore(initializeApp(firebaseConfig));
    const db={recetas:[],extras:[],stock:[],ventas:[]}; let producto=null,extrasElegidos=[],carrito=[];
    const $=id=>document.getElementById(id), fmt=n=>'$'+Math.round(n||0).toLocaleString('es-AR'), norm=s=>(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').trim().toLowerCase();
    function toast(msg){$('toast').textContent=msg;$('toast').classList.add('show');setTimeout(()=>$('toast').classList.remove('show'),2300)}
    function precioItem(){if(!producto)return 0;return producto.precio+extrasElegidos.reduce((sum,n)=>sum+(db.extras.find(e=>e.nombre===n)?.precio||0),0)}
    function total(){return carrito.reduce((sum,i)=>sum+i.subtotal,0)}
    function renderMenu(){$('productos').innerHTML=db.recetas.length?db.recetas.map(r=>`<button class="product ${producto?.id===r.id?'active':''}" data-id="${r.id}">${r.nombre}<b>${fmt(r.precio)}</b></button>`).join(''):'<span class="cart-empty">No hay productos cargados.</span>';document.querySelectorAll('.product').forEach(b=>b.onclick=()=>{producto=db.recetas.find(r=>r.id===b.dataset.id);extrasElegidos=[];renderMenu();renderExtras();$('agregar').disabled=false})}
    function renderExtras(){const card=$('extras-card');card.classList.toggle('hidden',!producto||!db.extras.length);$('extras').innerHTML=db.extras.map(e=>`<button class="extra ${extrasElegidos.includes(e.nombre)?'active':''}" data-nombre="${e.nombre}">${e.nombre} +${fmt(e.precio)}</button>`).join('');document.querySelectorAll('.extra').forEach(b=>b.onclick=()=>{const n=b.dataset.nombre;extrasElegidos=extrasElegidos.includes(n)?extrasElegidos.filter(x=>x!==n):[...extrasElegidos,n];renderExtras()})}
    function renderCarrito(){$('carrito').innerHTML=carrito.length?carrito.map((i,k)=>`<div class="cart-row"><div><b>${i.qty}× ${i.burger}</b><div class="cart-detail">${i.extras.length?i.extras.join(', '):'Sin extras'}</div></div><div><b>${fmt(i.subtotal)}</b><button class="btn secondary" data-borrar="${k}" aria-label="Quitar">×</button></div></div>`).join(''):'Todavía no agregaste productos.';document.querySelectorAll('[data-borrar]').forEach(b=>b.onclick=()=>{carrito.splice(+b.dataset.borrar,1);renderCarrito()});const t=total();$('total').textContent=fmt(t);$('confirmar').textContent=`Confirmar pedido · ${fmt(t)}`;$('confirmar').disabled=!carrito.length}
    function renderRecientes(){const lista=[...db.ventas].sort((a,b)=>String(b.fecha||'').localeCompare(String(a.fecha||''))).slice(0,5);$('recientes').innerHTML=lista.length?lista.map(v=>`<div class="recent-item"><div><b>${v.cliente||'Sin nombre'}</b><small>${(v.items||[]).map(i=>`${i.qty}× ${i.burger}`).join(' · ')}</small></div><b>${fmt(v.total)}</b></div>`).join(''):'Aún no hay pedidos.'}
    $('agregar').onclick=()=>{const qty=Math.max(1,parseInt($('cantidad').value)||1);carrito.push({burger:producto.nombre,qty,extras:[...extrasElegidos],subtotal:precioItem()*qty});$('cantidad').value=1;extrasElegidos=[];renderExtras();renderCarrito()};
    async function descontarStock(items){for(const item of items){const receta=db.recetas.find(r=>r.nombre===item.burger);const lineas=[...(receta?.ing||'').split(','),...item.extras.flatMap(nombre=>(db.extras.find(e=>e.nombre===nombre)?.ing||'').split(','))].filter(Boolean);for(const linea of lineas){const [nombre,cantTxt,unidadReceta]=linea.split(':').map(x=>x?.trim());const stock=db.stock.find(s=>norm(s.nombre)===norm(nombre));const base=parseFloat(cantTxt);if(!stock||!Number.isFinite(base))continue;let cant=base*item.qty;if(['g','gr'].includes(norm(unidadReceta))&&norm(stock.unidad)==='kg')cant/=1000;if(['ml','cc'].includes(norm(unidadReceta))&&norm(stock.unidad)==='l')cant/=1000;await runTransaction(fsDb,async tx=>{const ref=doc(fsDb,'stock',stock.id),snap=await tx.get(ref);if(snap.exists())tx.update(ref,{cantidad:Math.max(0,Math.round(((Number(snap.data().cantidad)||0)-cant)*1000)/1000)})})}}}
    $('confirmar').onclick=async()=>{const cliente=$('cliente').value.trim();if(!cliente){toast('Ingresá el nombre del cliente');$('cliente').focus();return}$('confirmar').disabled=true;try{const items=carrito.map(i=>({...i}));await addDoc(collection(fsDb,'ventas'),{fecha:new Date().toISOString().slice(0,10),cliente,items,total:total(),pago:$('pago').value,notas:$('notas').value.trim()});await descontarStock(items);carrito=[];$('cliente').value='';$('notas').value='';renderCarrito();toast('✓ Pedido enviado a la computadora')}catch(e){console.error(e);toast('No se pudo guardar. Revisá la conexión.')}finally{$('confirmar').disabled=!carrito.length}};
    ['recetas','extras','stock','ventas'].forEach(nombre=>onSnapshot(collection(fsDb,nombre),snap=>{db[nombre]=snap.docs.map(d=>({id:d.id,...d.data()}));$('status').textContent='● EN LÍNEA';if(nombre==='recetas')renderMenu();if(nombre==='extras')renderExtras();if(nombre==='ventas')renderRecientes()},()=>{$('status').textContent='● SIN CONEXIÓN'}));
  </script>
</body>
</html>
